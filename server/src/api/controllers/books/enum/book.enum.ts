export enum BookEnum {
  BOOK_NOT_FOUND = 'Book not found',
  UNKNOWN_SERVER_ERROR = 'Unknown server error',
}
export enum BookCodesEnum {
  SUCCESS = 200,
  NOT_AUTHORIZED = 401,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  ALREADY_EXIST = 409,
  INTERNAL_SERVER_ERROR = 500
}
